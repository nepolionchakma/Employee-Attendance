// Employees are matched by their Gmail address — NOT by name.
// All members are now managed in the Google Sheets "Members" tab.
// These fallback arrays are empty — the system reads from the sheet directly.
export const ADMIN_EMAILS: string[] = []

export const ALLOWED_EMAILS: string[] = []
